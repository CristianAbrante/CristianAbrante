{
  "name": "cristianabrante-resume",
  "version": "1.0.0",
  "description": "Multi-format resume generation from JSON Resume",
  "scripts": {
    "generate:readme": "node scripts/generate-readme.js",
    "generate:cv": "node scripts/generate-cv-typst.js",
    "generate:pdf": "typst compile cv/typst/cv.typ cv/typst/cv.pdf",
    "generate:website": "node scripts/generate-website.js",
    "generate:all": "npm run generate:readme && npm run generate:cv && npm run generate:pdf && npm run generate:website && npm run validate:ats",
    "validate:ats": "node scripts/validate-ats.js"
  },
  "dependencies": {
    "@jsonresume/schema": "^1.2.1"
  }
}
